# build output
dist/
.astro/

# dependencies
node_modules/

# NOTE: public/media is deliberately NOT ignored. Netlify builds with
# `npm run build` only — it has no Python or Pillow — so the generated
# derivatives must be committed or CI deploys ship a site with no photos.
# Regenerate locally with `npm run images` after adding originals.

# os / editor
.DS_Store
Thumbs.db
*.log
